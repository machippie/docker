# Standards: 0.1
---
# @var docker_user:description: User to run user-specific commands
# @var docker_user:value: docker_user | default(homebrew_user) | default(ansible_user_id)
# docker_user:

# @var docker_started:description: Start in background after install
docker_started: True

...
